// Lombok (compileOnly + annotationProcessor) is wired up automatically by the
// io.freefair.lombok plugin applied in the root build.gradle.

[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]

eclipse.project.name = appName + "-core"
